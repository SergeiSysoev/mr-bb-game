{
  "name": "mr-bb-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "lint": "node --check src/game.js && node --check src/game-logic.js && node --check vite.config.js",
    "test": "node --test"
  },
  "devDependencies": {
    "vite": "8.2.2"
  }
}
